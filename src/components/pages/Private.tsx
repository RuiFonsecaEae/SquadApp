import React from 'react';
import { PageContainer } from '../containers/PageContainer';

export const Private: React.FC = () => {
    return (
        <div>
            <PageContainer title="Private"></PageContainer>
        </div>
    );
};
